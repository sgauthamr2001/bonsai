type _option0 = struct _option0{ value : i32; set : bool }

func foo(i : (_option0*), j : (_option0*)) -> i32 {
  if (!(*i).set) {
    return 0
  } else if (!(*j).set) {
    return 0
  } else {
    return (*i).value + (*j).value
  }
}


