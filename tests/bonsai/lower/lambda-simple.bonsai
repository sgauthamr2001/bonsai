// This should *not* be changed.
func relu(v: set[i32]) = filter(|x : i32| x > 0, v);

func foo() -> i32 {
    L1 = |x: i32| x + 1;
    y: i32 = L1(0);
    return y;
}

func main() -> i32 {
    L1 = |x: i32| x - 1;
    y: i32 = L1(0);
    return y;
}
